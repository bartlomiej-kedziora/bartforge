server:
  port: 8080

bartforge:
  llm:
    ollama:
      base-url: http://localhost:11434
      model: tinyllama
      timeout-seconds: 30
    openai:
      base-url: https://api.openai.com/v1
      api-key: ${OPENAI_API_KEY}
      model: gpt-5-mini
      timeout-seconds: 30